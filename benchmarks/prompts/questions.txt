# 压测用问题集，每行一个。以 # 开头的行为注释。
# 问题应可由 doc-kvcache.txt 回答，且长度相近（避免输入长度差异干扰测量）。
为什么推理时需要缓存 K 和 V，却不需要缓存 Q？
KV Cache 的存储复杂度是 O(N) 还是 O(N²)？请说明原因。
num_key_value_heads 这个参数代表什么含义？它如何影响显存占用？
MLA 与 GQA 在 KV 存储方式上的根本差异是什么？
为什么 MLA 的计算公式中既不乘 2 也不乘头数？
滑动窗口注意力与 GQA 是同一类技术吗？请说明理由。
为什么每一层都需要独立缓存，不能只保留最后一层？
Prefill 与 Decode 两个阶段的瓶颈分别是什么？
为什么批处理能均摊模型权重的读取开销，却无法均摊 KV Cache？
前缀缓存命中时发生了什么？是否需要拷贝数据？
为什么可变内容必须放在提示词的末尾而不是开头？
正在解码的请求，其 KV Cache 能否卸载到 CPU 内存？为什么？
逐层预取在哪些场景下有效，哪些场景下无效？
DeepSeek 的 config.json 中有哪个字段容易被误用？会导致什么后果？
为什么权重量化不影响 KV Cache 的精度？
PagedAttention 分块设计的最初目的是什么？
思考模式对 KV Cache 造成的主要压力是什么类型？
GPT-OSS 的 head_dim 为什么不能由 hidden_size 除以头数推导？
从存储层带宽的角度看，为什么 KV 卸载的难点不在搬出去？
如果要降低单 token 的 KV 体积，有哪些技术路线可选？
